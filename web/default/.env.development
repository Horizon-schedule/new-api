# 开发仅使用 3000 端口（与线上一致）
#   cd web/default && bun run dev
#   访问 http://localhost:3000
# Go 同时提供页面与 /api，无需 Rsbuild dev 或 3001

VITE_DEV_SERVER_PORT=3000
VITE_REACT_APP_SERVER_URL=http://localhost:3000
