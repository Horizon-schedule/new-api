# 开发约定（推荐，与 docker-compose.dev.yml 思路一致）：
# - 浏览器访问 Rsbuild：http://localhost:3000
# - Go 后端勿占用 3000，请使用：PORT=3001 go run . （在仓库根目录）
# - 下方地址为 dev 代理目标（仅 /api、/mj、/pg）

VITE_DEV_SERVER_PORT=3000
VITE_REACT_APP_SERVER_URL=http://localhost:3001
